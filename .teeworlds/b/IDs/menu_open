echo ß: cancel/close 
bind minus exec "b/IDs/menu_close"

echo 1: marzzzello 
bind 1 exec "b/IDs/marzzzello"

echo 2: RAGEmarzzzello 
bind 2 exec "b/IDs/RAGEmarzzzello"

echo 3: wuseltier (switch)
bind 3 exec "b/IDs/wuseltier"

echo 4: LAG
bind 4 exec "b/IDs/LAG" 

echo 5: AFK
bind 5 exec "b/IDs/AFK" 

echo 6: AFK marzzzello
bind 6 exec "b/IDs/AFK marzzzello" 

echo 7: AFK wuseltier 
bind 7 exec "b/IDs/AFK wuseltier"

echo 8: Cpt 
bind 8 exec "b/IDs/Cpt"

echo 9: Cpt. marzzzello 
bind 9 exec "b/IDs/Cpt. marzzzello"

echo 0: Cpt. wuseltier 
bind 0 exec "b/IDs/Cpt. wuseltier"
